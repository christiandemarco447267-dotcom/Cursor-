# Optional: enable live Plaid banking data.
# Without these, Harbor runs in sandbox demo mode with realistic sample accounts.
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Optional: after exchanging a public token in live mode, persist an access token
# (production apps should store this in a database per user/item).
PLAID_ACCESS_TOKEN=
