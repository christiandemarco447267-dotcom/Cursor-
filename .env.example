# Optional: enable live quotes via Finnhub (server-side only).
# Without this key the app runs with deterministic demo prices.
# Never prefix this with NEXT_PUBLIC_ — it must not reach the browser.
FINNHUB_API_KEY=
